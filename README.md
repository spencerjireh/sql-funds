# sql-funds

Interactive SQL course that runs entirely in your browser. 25 lessons from `SELECT *` to window functions, each with a live query sandbox powered by sql.js -- no database setup required.

## Run locally

```sh
npm install
npm run dev
```

## Stack

React, Vite, sql.js (in-browser SQLite), CodeMirror, Framer Motion, React Three Fiber
